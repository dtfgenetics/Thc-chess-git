# Run Commands

Install dependencies:

```bash
pnpm install
```

Run both apps:

```bash
pnpm dev
```

Run only client:

```bash
pnpm dev:client
```

Run only server:

```bash
pnpm dev:server
```

Check client:

```bash
pnpm --filter client lint
pnpm build:client
```
