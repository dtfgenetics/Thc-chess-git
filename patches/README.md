# Historical patches

The patch files in this directory are retained for provenance and emergency recovery only.
The `GamePage.tsx` site URL and Kush Kings changes are already applied on
`fix/site-url-config`; production deployment must not reapply them.
